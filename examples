
 Addiing new repository:

  git init
  git remote add origin https://github.com/olegse/scripts
  git fetch
  git pull origin master
  git add .
  git commit -m ""
  git push --set-upstream origin

  

 Initializing existent repository in a directory. Do `git fetch` prior initialization.
